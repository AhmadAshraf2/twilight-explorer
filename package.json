{
  "name": "twilight-explorer",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@9.2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@twilight-explorer/web",
    "build": "npm run build --workspace=@twilight-explorer/web",
    "start": "npm run start --workspace=@twilight-explorer/web",
    "lint": "npm run lint --workspace=@twilight-explorer/web"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
